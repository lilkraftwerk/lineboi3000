import React from 'react';
import { render } from 'react-dom';
import FilterPlaygroundContainer from './FilterPlaygroundContainer';

render(
    <div>
        <FilterPlaygroundContainer />
    </div>,
    document.getElementById('root')
);
